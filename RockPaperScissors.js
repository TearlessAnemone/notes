// declare a function called add that accepts two arguments `x` and `y`
function add( x, y ) {
  // add arguments together and return to invoke
  return x + y;
}

// declare a and set it to the result (return value) of `add`
const a = add( 1, 2 );
console.log( a );
add( 3, 4 );
add( 4, 5 );
